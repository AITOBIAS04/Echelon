[project]
name = "prediction-market-monorepo"
requires-python = ">=3.9"
dependencies = [
    "pydantic>=2.0",
    "httpx>=0.24",
    "PyYAML>=6.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
]

[tool.pytest.ini_options]
pythonpath = ["."]
